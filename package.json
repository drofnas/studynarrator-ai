{
  "name": "studynarrator",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "packageManager": "npm@11.19.0",
  "engines": {
    "node": ">=24.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @studynarrator/web && npm run build --workspace @studynarrator/server && npm run build --workspace @studynarrator/desktop",
    "db:migrate": "npm run build --workspace @studynarrator/server && npm run migrate --workspace @studynarrator/server --",
    "dev:web": "concurrently --kill-others --names api,web \"npm run dev --workspace @studynarrator/server\" \"npm run dev --workspace @studynarrator/web\"",
    "dev:desktop": "npm run build --workspace @studynarrator/desktop && npm run rebuild:native --workspace @studynarrator/desktop && concurrently --kill-others --names web,desktop \"npm run dev --workspace @studynarrator/web\" \"wait-on http://127.0.0.1:5173 && npm run start --workspace @studynarrator/desktop\"",
    "fake:speaches": "npm run start --workspace @studynarrator/fake-speaches",
    "fake:speaches:inspect": "node scripts/fake-speaches/control.mjs inspect",
    "fake:speaches:reset": "node scripts/fake-speaches/control.mjs reset",
    "fake:speaches:scenario": "node scripts/fake-speaches/control.mjs scenario",
    "e2e:install": "playwright install chromium",
    "audit:dead-code": "node scripts/audit-dead-code.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint . --max-warnings 0",
    "test": "vitest run",
    "test:api": "vitest run --config vitest.api.config.ts",
    "test:coverage": "vitest run --coverage",
    "test:e2e:web": "npm run build --workspace @studynarrator/web && playwright test --project=web",
    "test:e2e:electron": "npm run build --workspace @studynarrator/web && npm run build --workspace @studynarrator/desktop && npm run rebuild:native --workspace @studynarrator/desktop && playwright test --project=electron",
    "test:e2e": "npm run build --workspace @studynarrator/web && npm run build --workspace @studynarrator/desktop && npm run rebuild:native --workspace @studynarrator/desktop && playwright test",
    "typecheck": "tsc --build --pretty false",
    "verify": "node scripts/verify.mjs",
    "verify:docker": "node scripts/verify-docker.mjs"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@playwright/test": "1.62.1",
    "@testing-library/jest-dom": "7.0.1",
    "@testing-library/react": "16.3.2",
    "@testing-library/user-event": "14.6.3",
    "@types/node": "24.13.3",
    "@types/react": "19.2.18",
    "@types/react-dom": "19.2.4",
    "@vitest/coverage-v8": "4.1.10",
    "concurrently": "10.0.4",
    "eslint": "10.8.1",
    "fflate": "0.8.2",
    "globals": "17.9.0",
    "jsdom": "30.0.1",
    "prettier": "^3.9.6",
    "tsx": "4.23.12",
    "typescript": "6.0.3",
    "typescript-eslint": "8.67.0",
    "vitest": "4.1.10",
    "wait-on": "9.1.0"
  }
}
